<h1>The Jobs EJS Application</h1>

<% if (errors && errors.length) { %>
    <% errors.forEach((err)=> { %>
        <div>Error: <%= err %>
        </div>
        <% }) %>
            <% } %>

                <% if (info && info.length) { %>
                    <% info.forEach((msg)=> { %>
                        <div>Info: <%= msg %>
                        </div>
                        <% }) %>
                            <% } %>

                                <hr>